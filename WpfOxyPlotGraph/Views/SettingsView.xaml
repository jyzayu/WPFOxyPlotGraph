<Page x:Class="WpfOxyPlotGraph.Views.SettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="설정">
  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel Margin="16" Orientation="Vertical" >
      <TextBlock Text="병원 정보" FontWeight="Bold" Margin="0,0,0,8"/>
      <Grid Margin="0,0,0,16">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="120"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="이름" Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Hospital.Name, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>
        <TextBlock Text="주소" Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Hospital.Address, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>
        <TextBlock Text="전화" Grid.Row="2" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Hospital.Phone, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>
        <TextBlock Text="대표자" Grid.Row="3" Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Hospital.Director, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="1"/>
      </Grid>

      <TextBlock Text="문서 템플릿" FontWeight="Bold" Margin="0,0,0,8"/>
      <DockPanel Margin="0,0,0,16">
        <TextBox Text="{Binding DocumentTemplatesPath, UpdateSourceTrigger=PropertyChanged}" DockPanel.Dock="Left" Width="400" Margin="0,0,8,0"/>
        <Button Content="찾아보기" Command="{Binding BrowseTemplatesPathCommand}"/>
      </DockPanel>

      <TextBlock Text="기본 프린터" FontWeight="Bold" Margin="0,0,0,8"/>
      <ComboBox ItemsSource="{Binding Printers}" SelectedItem="{Binding SelectedPrinter}" Width="400" Margin="0,0,0,16"/>

      <StackPanel Orientation="Horizontal">
        <Button Content="저장" Width="100" Command="{Binding SaveCommand}" />
        <Button Content="다시 불러오기" Width="120" Margin="8,0,0,0" Command="{Binding ReloadCommand}" />
      </StackPanel>
    </StackPanel>
  </ScrollViewer>
</Page>


