<Page x:Class="WpfOxyPlotGraph.Views.PrescriptionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="PrescriptionView">
  <Page.Resources>
    <Style x:Key="BaseStyle" TargetType="{x:Type Control}">
      <Setter Property="FontSize" Value="20"/>
      <Setter Property="Margin" Value="6"/>
    </Style>
    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource BaseStyle}"/>
    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource BaseStyle}"/>
    <Style TargetType="{x:Type Label}" BasedOn="{StaticResource BaseStyle}"/>
    <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource BaseStyle}"/>
  </Page.Resources>

  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="처방 등록" FontSize="28" Margin="10,10,10,10"/>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="250"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <Label Grid.Row="0" Grid.Column="0" Content="약품"/>
      <ComboBox Grid.Row="0" Grid.Column="1"
                ItemsSource="{Binding Medications}"
                SelectedItem="{Binding SelectedMedication, Mode=TwoWay}"
                DisplayMemberPath="Name"
                Width="250"/>

      <Label Grid.Row="1" Grid.Column="0" Content="현재 재고"/>
      <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CurrentStock}" VerticalAlignment="Center"/>

      <Label Grid.Row="2" Grid.Column="0" Content="처방 수량"/>
      <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
        <TextBox Width="250" Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"/>
      </StackPanel>

      <Label Grid.Row="3" Grid.Column="0" Content="처방 후 예상 재고"/>
      <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ProjectedStock}" VerticalAlignment="Center"/>

      <Label Grid.Row="4" Grid.Column="0" Content="알림"/>
      <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding AlertMessage}" Foreground="Tomato" TextWrapping="Wrap"/>
    </Grid>

    <Border Grid.Row="2" Background="#FFF" BorderThickness="1,1,1,0" BorderBrush="#DDD" Padding="10,8">
      <WrapPanel HorizontalAlignment="Right">
        <Button Content="샘플 데이터 초기화" Command="{Binding InitSeedCommand}"/>
        <Button Content="처방 저장" Command="{Binding SavePrescriptionCommand}" Margin="10,0,0,0"/>
      </WrapPanel>
    </Border>
  </Grid>
</Page>



