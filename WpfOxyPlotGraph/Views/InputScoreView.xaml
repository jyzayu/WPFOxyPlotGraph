<Page x:Class="WpfOxyPlotGraph.Views.InputScoreView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfOxyPlotGraph.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InputScoreView">
    <Page.Resources>
        <Style x:Key="BaseStyle" TargetType="{x:Type Control}">
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Page.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="200"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="성적 입력" FontSize="28" Margin="10,10,10,0"/>

    <TextBlock Grid.Row="1" Grid.Column="0" Text="학생" VerticalAlignment="Center" Margin="10,5"/>
    <ComboBox Grid.Row="1" Grid.Column="1"
              ItemsSource="{Binding Students}"
              SelectedItem="{Binding SelectedStudent, Mode=TwoWay}"
              DisplayMemberPath="Name"
              Style="{StaticResource BaseStyle}"
              MinWidth="200"/>

    <TextBlock Grid.Row="2" Grid.Column="0" Text="일자" VerticalAlignment="Center" Margin="10,5"/>
    <DatePicker Grid.Row="2" Grid.Column="1"
                SelectedDate="{Binding Date, Mode=TwoWay}"
                Style="{StaticResource BaseStyle}"/>

    <TextBlock Grid.Row="3" Grid.Column="0" Text="국어" VerticalAlignment="Center" Margin="10,5"/>
    <TextBox Grid.Row="3" Grid.Column="1"
             Text="{Binding KorScore, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
             Style="{StaticResource BaseStyle}"/>

    <TextBlock Grid.Row="4" Grid.Column="0" Text="수학" VerticalAlignment="Center" Margin="10,5"/>
    <TextBox Grid.Row="4" Grid.Column="1"
             Text="{Binding MathScore, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
             Style="{StaticResource BaseStyle}"/>

    <TextBlock Grid.Row="5" Grid.Column="0" Text="영어" VerticalAlignment="Center" Margin="10,5"/>
    <TextBox Grid.Row="5" Grid.Column="1"
             Text="{Binding EngScore, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
             Style="{StaticResource BaseStyle}"/>

    <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
      <Button Content="저장" Command="{Binding SaveCommand}" Style="{StaticResource BaseStyle}" />
      <Button Content="초기화" Command="{Binding ClearCommand}" Style="{StaticResource BaseStyle}" />
    </StackPanel>
  </Grid>

</Page>
