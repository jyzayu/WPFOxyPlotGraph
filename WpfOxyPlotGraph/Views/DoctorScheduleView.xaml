<Page x:Class="WpfOxyPlotGraph.Views.DoctorScheduleView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:WpfOxyPlotGraph.Views"
      mc:Ignorable="d"
      Title="DoctorScheduleView">
  <Page.Resources>
    <Style x:Key="BaseStyle" TargetType="{x:Type Control}">
      <Setter Property="FontSize" Value="20"/>
      <Setter Property="Margin" Value="5"/>
    </Style>
    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource BaseStyle}"/>
    <Style TargetType="{x:Type Button}" BasedOn="{StaticResource BaseStyle}"/>
    <Style TargetType="{x:Type Label}" BasedOn="{StaticResource BaseStyle}"/>
    <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource BaseStyle}"/>
    <Style TargetType="{x:Type ListBox}" BasedOn="{StaticResource BaseStyle}"/>
  </Page.Resources>

  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="의사별 예약 현황" FontSize="28" Margin="10,10,10,10"/>

    <!-- Filters -->
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="300"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="220"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Label Grid.Column="0" Content="담당의사"/>
      <ComboBox Grid.Column="1"
                ItemsSource="{Binding Doctors}"
                IsEditable="True"
                Text="{Binding SelectedDoctor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

      <Label Grid.Column="2" Content="조회일자"/>
      <DatePicker Grid.Column="3" SelectedDate="{Binding SelectedDate, Mode=TwoWay}" />
    </Grid>

    <!-- Schedule -->
    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <TextBlock Grid.Row="0" Grid.Column="0" Text="예약된 시간" FontWeight="Bold" Margin="0,0,0,5"/>
      <TextBlock Grid.Row="0" Grid.Column="2" Text="예약 가능 시간" FontWeight="Bold" Margin="0,0,0,5"/>

      <ListBox Grid.Row="1" Grid.Column="0"
               ItemsSource="{Binding BookedTimeSlots}" />

      <Separator Grid.Row="1" Grid.Column="1" Width="20" Background="Transparent"/>

      <ListBox Grid.Row="1" Grid.Column="2"
               ItemsSource="{Binding AvailableTimeSlots}"
               SelectedItem="{Binding SelectedAvailableSlot, Mode=TwoWay}" />
    </Grid>

    <!-- Booking form -->
    <Border Grid.Row="3" Background="#FFF" BorderThickness="1,1,1,0" BorderBrush="#DDD" Padding="10,8" Margin="0,10,0,0">
      <WrapPanel HorizontalAlignment="Right">
        <Label Content="환자"/>
        <ComboBox Width="260"
                  ItemsSource="{Binding Patients}"
                  SelectedItem="{Binding SelectedPatient, Mode=TwoWay}"
                  DisplayMemberPath="Name"/>
        <Label Content="사유" Margin="10,0,0,0"/>
        <TextBox Width="240" Text="{Binding Reason, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Content="메모" Margin="10,0,0,0"/>
        <TextBox Width="240" Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Content="예약 등록" Command="{Binding BookAppointmentCommand}" Margin="10,0,0,0"/>
      </WrapPanel>
    </Border>
  </Grid>
</Page>


